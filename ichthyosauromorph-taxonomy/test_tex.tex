% !TEX program = lualatex

\documentclass{article}

\usepackage{xparse}

\usepackage{parskip}
\usepackage{tabularx}

\usepackage[%
    backend      = biber,
    style        = authoryear,
    maxcitenames = 2,
    mincitenames = 1,
    maxbibnames  = 100,
    alldates     = year,
    uniquename   = false,
    uniquelist   = false,
    giveninits   = true,
    mergedate    = maximum
]{biblatex}
\addbibresource{zotero.bib}

\ProvideDocumentCommand{\cauth}{ m }{\citeauthor*{#1}}
\ProvideDocumentCommand{\pauth}{ m }{(\citeauthor*{#1})}
\ProvideDocumentCommand{\cyear}{ m }{\cite*{#1}}
\ProvideDocumentCommand{\pyear}{ m }{\parencite*{#1}}

\begin{document}
    \input {write_file.tex}

    \printbibliography
\end{document}
